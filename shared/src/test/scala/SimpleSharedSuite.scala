import utest._


/**
 * Contains tests for class user and its validations
 */
object SimpleSharedSuite extends utest.TestSuite {
  def tests = TestSuite {
    "the low me or die" - {
      assert(true)
    }
  }

}
